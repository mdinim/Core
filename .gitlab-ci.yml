stages:
    - build
    - test

before_script:
    - apt-get update -qq && apt-get install -y -qq gcc make cmake
    - gcc -v
    - cmake --version

build:
    stage: build
    script: "echo Hello World!"

test:
    stage: test
    script: "echo Test World!"

